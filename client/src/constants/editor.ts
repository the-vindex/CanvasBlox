// Tile size in pixels - each tile in the game world is rendered as this many pixels
export const TILE_SIZE = 32;

// Default canvas size in tiles for a typical platformer level (2-3 minutes to complete)
export const DEFAULT_LEVEL_WIDTH_TILES = 60;
export const DEFAULT_LEVEL_HEIGHT_TILES = 30;

// Default grass platform position (10 blocks from bottom)
export const DEFAULT_GRASS_Y = 20;
